export type TicketStatus = 'active' | 'closed' | 'paused';

export const TicketStatus = {
    ACTIVE: 'active' as TicketStatus,
    CLOSED: 'closed' as TicketStatus,
    PAUSED: 'paused' as TicketStatus
}